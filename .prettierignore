dist
node_modules
coverage
package-lock.json

# `sam build` artifacts, gitignored, not source (node P2-N014). Since
# Prettier's CLI already follows `.gitignore` by default, this entry
# is a no-op today — listed explicitly anyway so the exclusion does
# not silently depend on that fallback (same reasoning eslint.config.js
# and .gitignore already apply to this directory).
.aws-sam

# A byte-for-byte copy of the coordinating repository's real
# docs/plan-register.md (test/planRegisterParser.test.ts's round-trip
# fixture, node P2-N009) — must stay untouched by Prettier's Markdown
# emphasis-style rewriting or it stops being a verbatim copy.
test/fixtures/plan-register.sample.md

# The vendored shared grammar unit and its conformance corpus
# (RU-012, node P2-N010): generated byte-for-byte by
# sync_shared_unit.ts's --check drift mode from the canonical copy in
# majodali/project-orchestrator. Prettier reformatting it here would
# make it drift from that canonical copy for no reason other than this
# repository's own style config — see src/planRegister/vendored/GENERATED.md.
src/planRegister/vendored/
