/**
 * The conformance corpus manifest (P1-N009 child B, node P1-N011,
 * spec B1/T3).
 *
 * Lists every fixture under `fixtures/`, the rule(s) it provokes (or,
 * for the two fixtures that are not checker-rule fixtures,
 * what it is for instead), and a one-line description. `run_corpus.ts`
 * reads this to know what to run and what to check; `sync_shared_unit.ts`
 * copies the whole `corpus/` directory (this file included) as part of
 * the travelling set (T3).
 *
 * Coverage claim (verifier check, spec B1): the distinct rule IDs the
 * corpus produces equal the checker's declared rule set — the eleven
 * rule names the form checker can emit (`register-parse`, `register-id`,
 * `register-stage`, `register-structure`, `backlog-ref`, `costlog-form`,
 * `journal-form`, `journal-crosscheck`, `liveness`, `rulings`,
 * `definitions`) — plus the not-enrolled case (no rule; informational,
 * exit 0) and the live tree (no rule; clean, exit 0). `run_corpus.ts`
 * computes this equality at runtime rather than trusting this comment;
 * the comment is orientation for a reader, not the check itself.
 *
 * This file names no script by its filename or interpreter (spec C2's
 * permitted-survivors rule, guarded against early by this child's own
 * criterion 6) — every fixture, this manifest, and every expectation
 * file describe the checker and the journal_tail implementations
 * generically rather than by filename. `run_corpus.ts` and
 * `sync_shared_unit.ts` are harness tools rather than
 * fixture/manifest/expectation data, and are outside this rule's
 * scope: before the P1-N013 cutover, `run_corpus.ts` invoked
 * `form_check.py` directly by its real path; after it, `run_corpus.ts`
 * calls this repository's own checker in-process and `sync_shared_unit.ts`
 * still names `sync_agents.py` in a provenance comment recording what
 * `sync_agents.ts` was ported from.
 */

// Generated by sync_shared_unit.ts — do not edit here; edit the
// primary copy in majodali/project-orchestrator and re-run.

export type FixtureKind =
  "form-check-rule" | "not-enrolled" | "live-tree" | "journal-tail-divergence";

export interface FixtureEntry {
  /** Directory name under fixtures/. */
  id: string;
  kind: FixtureKind;
  /**
   * rule IDs this fixture provokes, in the order its
   * findings are expected to appear. Empty for the non-rule kinds.
   */
  rules: string[];
  /** One line: what the fixture is and why it exists. */
  description: string;
}

export const FIXTURES: FixtureEntry[] = [
  {
    id: "register-parse",
    kind: "form-check-rule",
    rules: ["register-parse"],
    description:
      "A node-like line whose stage bracket never closes — the grammar rejects it, does not silently skip it.",
  },
  {
    id: "register-id",
    kind: "form-check-rule",
    rules: ["register-id"],
    description: "The same node ID used twice in the register.",
  },
  {
    id: "register-stage-verifiying",
    kind: "form-check-rule",
    rules: ["register-stage"],
    description:
      "D2 — the motivating disagreement: a misspelled [verifiying] stage, encoding the P2-N009 finding that the service's parser passes an out-of-vocabulary stage through as fact while the form checker rejects it. Both readings are recorded in expectations/register-stage-verifiying.json.",
  },
  {
    id: "register-structure-interior-wrong-stage",
    kind: "form-check-rule",
    rules: ["register-structure"],
    description:
      "A node with children whose stage is not broken-down/verifying/done (one of register-structure's two arms).",
  },
  {
    id: "register-structure-broken-down-no-children",
    kind: "form-check-rule",
    rules: ["register-structure"],
    description:
      "A [broken-down] node with no children at all (register-structure's other arm).",
  },
  {
    id: "backlog-missing-file",
    kind: "form-check-rule",
    rules: ["backlog-ref"],
    description:
      "docs/plan-register.md exists but docs/backlog.md does not, at all.",
  },
  {
    id: "backlog-missing-entry",
    kind: "form-check-rule",
    rules: ["backlog-ref"],
    description:
      "A node whose stage needs a Backlog entry, and docs/backlog.md exists but never mentions it.",
  },
  {
    id: "costlog-malformed",
    kind: "form-check-rule",
    rules: ["costlog-form", "costlog-form", "costlog-form"],
    description:
      "A duplicate task ID, a malformed task ID, and a malformed row (wrong cell count), in one Cost log.",
  },
  {
    id: "costlog-nonsequential",
    kind: "form-check-rule",
    rules: ["costlog-form"],
    description:
      "The costlog-form *warning* arm: valid, unique task IDs with a gap in the sequence (exit 0 — a warning is not a violation).",
  },
  {
    id: "journal-malformed",
    kind: "form-check-rule",
    rules: ["journal-form", "journal-form", "journal-form"],
    description:
      "Invalid JSON, a line missing a required field, and an unknown event kind, one per journal line.",
  },
  {
    id: "journal-crosscheck",
    kind: "form-check-rule",
    rules: ["journal-crosscheck", "journal-crosscheck"],
    description:
      "Both directions: a Cost log row with no accepted journal event, and an accepted journal event with no Cost log row.",
  },
  {
    id: "liveness",
    kind: "form-check-rule",
    rules: ["liveness", "liveness"],
    description:
      "Both arms: a stale hold marker on a [done] node, and an [executing] node with neither a hold marker nor an open dispatched task.",
  },
  {
    id: "rulings-no-register",
    kind: "form-check-rule",
    rules: ["rulings"],
    description:
      "The journal has a precedent-applied event but there is no docs/rulings.md at all.",
  },
  {
    id: "rulings-multiple",
    kind: "form-check-rule",
    rules: ["rulings", "rulings", "rulings"],
    description:
      "An undefined ruling, a superseded (inactive) ruling applied anyway, and a ruling applied but missing from its own Applied list.",
  },
  {
    id: "definitions-no-classification",
    kind: "form-check-rule",
    rules: ["definitions"],
    description:
      "An enrolled project (a register exists) with no docs/classification.md at all.",
  },
  {
    id: "definitions-missing-markers",
    kind: "form-check-rule",
    rules: ["definitions", "definitions", "definitions", "definitions"],
    description:
      "Plan register, Cost log, Run journal and Ruling register instances all exist, and the Classification's Custom definitions section cites none of them.",
  },
  {
    id: "not-enrolled",
    kind: "not-enrolled",
    rules: [],
    description:
      "No docs/plan-register.md at all — the informational, exit-0, not-enrolled case.",
  },
  {
    id: "live-tree",
    kind: "live-tree",
    rules: [],
    description:
      "A snapshot of this repository's own live docs/ and orchestration/ at this child's last commit — the corpus's one fixture that passes clean.",
  },
  {
    id: "journal-tail-duplicate-id",
    kind: "journal-tail-divergence",
    rules: [],
    description:
      "Not a checker-rule fixture: two node lines sharing one ID, run through the Python and TypeScript journal_tail implementations, to make the duplicate-ID divergence visible (docs/backlog.md, 'journal_tail's duplicate-ID behaviour changed in the port'). Deciding which behaviour is correct is needs-judgment, not this child's — see journal-tail-duplicate-id/README.md.",
  },
];

/** The eleven rule IDs the form checker's find() call sites can emit. */
export const DECLARED_RULE_SET = [
  "register-parse",
  "register-id",
  "register-stage",
  "register-structure",
  "backlog-ref",
  "costlog-form",
  "journal-form",
  "journal-crosscheck",
  "liveness",
  "rulings",
  "definitions",
] as const;
